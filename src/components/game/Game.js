import React, { Component } from 'react';
import Man from './Man';

export default class Game extends Component {
  render() {
    return (
      <section>
        <Man />
      </section>
    );
  }
}